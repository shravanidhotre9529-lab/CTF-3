import React from 'react';

export default function Home() {
  return (
    <main style={{ fontFamily: 'sans-serif', padding: 40 }}>
      <h1>Theme Configurator (Next.js)</h1>
      <p>The API endpoint is available at <code>/api/challenge-3/save-theme</code>.</p>
      <p>Use the frontend UI from your original index.html or call the API directly with POST JSON.</p>
    </main>
  );
}
